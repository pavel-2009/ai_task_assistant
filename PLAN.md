# PLAN

## Текущее состояние проекта (2026-04-15)

### ✅ Завершено
- Реализованы основные API-роутеры: `auth`, `tasks`, `nlp`, `rag`, а также дополнительные модули (`avatars`, `recsys`, `monitoring`, `streaming`).
- Вынесена сервисная бизнес-логика в `app/services` (`AuthService`, `TaskService`, `UserService`).
- Настроены unit/integration тесты для критичных пользовательских сценариев.
- Добавлено расширенное unit-покрытие для сервисного слоя и edge-case сценариев роутеров.

### 🔄 В процессе
- Расширение интеграционных тестов для модулей, зависящих от внешних ML/очередей и окружения.
- Дальнейшее повышение покрытия негативных сценариев (ошибки инфраструктуры, таймауты, деградация внешних сервисов).

### 📌 Следующие шаги
1. Добавить CI-гейт на минимальный порог тестового покрытия (например, `--cov-fail-under`).
2. Добавить отдельные smoke/e2e сценарии для streaming-роутов и Celery-задач.
3. Сформировать матрицу тестирования по слоям (schemas/services/routers/tasks).
